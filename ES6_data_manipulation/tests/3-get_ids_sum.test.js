/* eslint-disable */
import getStudentIdsSum from '../3-get_ids_sum';

describe("getStudentsIdsSum", () => {
  test("should return a sum of Ids", () => {
    const ids = getStudentIdsSum();
    expect(ids).toBe(ids);
  });
});
